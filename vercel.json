{
  "buildCommand": "cd netflix-clone-ui && npm install && npm run build",
  "outputDirectory": "netflix-clone-ui/dist",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
